{
  "name": "masterymap-nextgen-mobile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "android:add": "npm run build && npx cap add android",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npm run android:sync && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "8.5.1",
    "@capacitor/core": "8.5.1",
    "@revenuecat/purchases-capacitor": "13.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.1",
    "vite": "latest"
  }
}
